domain文件下面对应数据库表,图解在https://www.processon.com/diagraming/5ba1e7e2e4b0fe81b63d7f49

Hue端内容记录表（HueShell):
id  主键，自增
name 名称
type 类型
input 父节点
output 输出名
gmt_create 创建时间
gmt_modify 修改时间
execute_rate 执行频率（h/d）
execute_time 具体执行点
shellname   脚本名称
shellcontent 脚本内容
paraments 参数

create table hueshell(id INT UNSIGNED AUTO_INCREMENT,name varchar(50),type varchar(10),input varchar(200),output varchar(50),gmt_create varchar(20),gmt_modify varchar(20),execute_rate varchar(10),execute_time varchar(40),shellname varchar(50),shellcontent varchar(3000),paraments varchar(50) ,PRIMARY KEY (id));

作业计划表(JobPlan)：
id 主键，自增
name 名称
shellname 要执行的脚本名称
input 父节点
execute_rate 执行频率（h/d) 下周讨论下这个字段的问题
execute_time 具体执行点
output 输出名
paraments：参数
gmt_create 创建时间
gmt_modify 修改时间
create table jobplan(id INT UNSIGNED AUTO_INCREMENT,name varchar(50),shellname varchar(50),input varchar(100),execute_rate varchar(5),execute_time varchar(40),output varchar(20),paraments varchar(50),gmt_create varchar(20),gmt_modify varchar(20),PRIMARY KEY (id));

脚本内容表(ShellContent)：
id  主键，自增
shellname 脚本名称
shellcontent 脚本内容
shelltype 脚本类型
gmt_create 创建时间
gmt_modify 修改时间
create table shellcontent(id INT UNSIGNED AUTO_INCREMENT,shellname varchar(30),shellcontent varchar(1000),shelltype varchar(10),gmt_create varchar(20),gmt_modify varchar(20),PRIMARY KEY (id));


任务实例表(TaskInstance)：
id: 主键，自增
name :名称    作业计划的name_hour
input:父节点
output：别名  作业计划的output_hour
gmt_create：创建时间
gmt_modify: 修改时间
status：状态码
execute_time：执行时间（小时）
execute_day:执行时间（天）
paraments：参数
start_time: 开始时间
end_time :结束时间
create table taskinstance(id INT UNSIGNED AUTO_INCREMENT,name varchar(50),input varchar(100),output varchar(10),gmt_create varchar(20),gmt_modify varchar(20),status int,execute_time varchar(50),execute_day varchar(10),paraments varchar(50),PRIMARY KEY (id),start_time varchar(20),end_time varchar(20));


执行实例表（ExecuteInstance）
id :主键，自增
parent_id：对应的任务实例ID
start_time：开始时间
end_time：结束时间
gmt_create 创建时间
gmt_modify 修改时间
status：状态码
log_link：日志链接

create table executeinstance(id INT UNSIGNED AUTO_INCREMENT,parant_id int,start_time varchar(20),end_time varchar(20),gmt_create varchar(20),gmt_modify varchar(20),status int,log_link varchar(200),PRIMARY KEY (id));